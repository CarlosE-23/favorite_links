<div class="container p-4">
  <div class="row">
    <div class="col-md-4 mx-auto">
      <div class="card text-center">
        <div class="card-header bg-primary text-white">
          <h4>Welcome!</h4>
        </div>
        <div class="card-body">
          <h3 class="card-title">{{user.fullname}}</h3>
          <h5 class="card-subtitle mb-2 text-muted">{{user.username}}</h5>
          <p class="card-text">Here you can save all your important links in one
            place.</p>
          <a href="/links" class="btn btn-primary m-4">Save your links</a>
        </div>
        <div class="card-footer text-muted">
          Enjoy your stay!
        </div>
      </div>
    </div>
  </div>
</div>